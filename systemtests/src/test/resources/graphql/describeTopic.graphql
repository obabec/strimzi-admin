query Topic($name: String!){
    topic(name: $name) {
        name
        isInternal
        config {
            key
            value
        }
        partitions {
            partition
            replicas {
                id
            }
            isr {
                id
            }
        }
    }
}